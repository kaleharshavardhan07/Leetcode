class Solution {
public:
    int countDaysTogether(string arriveAlice, string leaveAlice, string arriveBob, string leaveBob) {
        
        string am=arriveAlice.substr(0,2);
        string am=arriveAlice.substr(0,2);
        string am=arriveAlice.substr(0,2);
    }
};